export default function getStyle(data) {
    let ratio = 8;
    let signleHeight = data.height / 2 / 4;
    let signleWidth = data.width / 2 / 3;
    return {
        container: {
            width: data.width,
            height: data.height,
            borderRadius: 12,
            paddingLeft: data.width * 0.03,
            paddingRight: data.width * 0.03,
        },
        rankList: {
            width: Math.ceil(data.width * 0.94),
            height: data.height,
        },
        list: {
            width: Math.ceil(data.width * 0.94),
            height: data.height,
        },
        listItem: {
            position: 'relative',
            width: Math.ceil(data.width * 0.99),
            height: signleHeight,
            flexDirection: 'row',
            alignItems: 'center',
            marginTop: 2,
        },
        rankBg: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: Math.ceil(data.width * 0.94),
            height: signleHeight,
        },
        rankIcon: {
            position: 'absolute',
            top: signleHeight * 0.5 - signleHeight * 1.1 * 0.5 * 0.55,
            left: data.width * 0.05,
            width: signleHeight * 0.93 * 0.6,
            height: signleHeight * 1.1 * 0.6,
        },
        rankNum: {
            position: 'absolute',
            top: signleHeight * 0.45,
            left: data.width * 0.05 + signleHeight * 0.93 * 0.6 * 0.4,
            width: data.width * 0.15,
            height: (data.height / ratio) * 0.3,
            lineHeight: (data.height / ratio) * 0.3,
            fontSize: data.width * 0.048,
            color: '#000',
            verticalAlign: 'middle',
            fontWeight: 'bold',
        },

        rankAvatar: {
            borderRadius: data.width * 0.07,
            marginLeft: data.width * 0.18 + (data.height / 2 / 3) * 0.1,
            width: (data.height / 2 / 3) * 0.5,
            height: (data.height / 2 / 3) * 0.5,
        },
        rankName: {
            position: 'absolute',
            top: -signleHeight * 0.15,
            left: data.width * 0.055,
            width: data.width * 0.35,
            height: signleHeight * 0.4,
            textAlign: 'left',
            lineHeight: signleHeight * 0.4,
            fontSize: data.width * 0.052,
            textOverflow: 'ellipsis',
            color: '#7C8A97',
            fontWeight: 'bold',
        },
        rankScoreVal: {
            position: 'absolute',
            top: -signleHeight * 0.15,
            left: data.width * 0.15,
            width: data.width * 0.35,
            height: signleHeight * 0.4,
            textAlign: 'right',
            lineHeight: signleHeight * 0.4,
            fontSize: data.width * 0.052,
            color: '#000',
            fontWeight: 'bold',
        },
        shareNameView: {
            position: 'relative',
            marginLeft: data.width * 0.06,
            width: data.width * 0.35,
            height: (data.height / 2 / 3) * 0.4,
        },
        shareNameBg: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: data.width * 0.35,
            height: (data.height / 2 / 3) * 0.4,
        },
        shareName: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: data.width * 0.35,
            height: (data.height / 2 / 3) * 0.4,
            textAlign: 'center',
            lineHeight: (data.height / 2 / 3) * 0.4,
            fontSize: data.width * 0.043,
            textOverflow: 'ellipsis',
            color: '#fff',
        },
        shareToBtn: {
            position: 'relative',
            marginLeft: data.width * 0.08,
            width: data.width * 0.21,
            height: data.height * 0.16,
        },
        shareBtnBg: {
            position: 'absolute',
            right: 0,
            top: data.height * 0.16 * 0.25,
            width: data.width * 0.21,
            height: data.height * 0.16 * 0.5,
        },
        shareText: {
            position: 'absolute',
            right: 0,
            top: data.height * 0.16 * 0.25,
            width: data.width * 0.21,
            height: data.height * 0.16 * 0.5,
            lineHeight: data.height * 0.16 * 0.5,
            textAlign: 'center',
            fontSize: data.width * 0.043,
            color: '#fff',
        },
    };
}
