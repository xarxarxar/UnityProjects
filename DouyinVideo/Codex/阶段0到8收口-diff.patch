diff --git a/Codex/修改记录.md b/Codex/修改记录.md
index b3ac362..8ae04ef 100644
--- a/Codex/修改记录.md
+++ b/Codex/修改记录.md
@@ -56,6 +56,7 @@
 | 2026-06-17 | 阶段 8 第三批 | `Assets/人物信息/火墙火男/FireWallFireManRole.cs`、`Codex/阶段8-第三批-火墙火男结构整理.md`、`Codex/阶段8-第三批-diff.patch`、`Codex/安全重构计划.md`、`Codex/修改记录.md` | 复杂英雄结构整理/配置保护/文档/diff | 否 | 中低 | 整理火墙火男大招脚本结构，提取协程辅助方法，并保护火焰帧配置异常 | 编译项目；验证火墙等待、延伸、持续、伤害频率、回血流程、火焰动画和 `O` 键调试入口 | 按 `Codex/阶段8-第三批-diff.patch` 回滚 `FireWallFireManRole.cs`，并恢复本批 Markdown 改动 |
 | 2026-06-17 | 阶段 8 第四批 | `Assets/人物信息/迪迦/DiJiaRole.cs`、`Codex/阶段8-第四批-迪迦结构整理.md`、`Codex/阶段8-第四批-diff.patch`、`Codex/安全重构计划.md`、`Codex/修改记录.md` | 复杂英雄结构整理/常量提取/文档/diff | 否 | 中低 | 整理迪迦持续激光大招结构，提取常量、激光方向、命中判断、线段刷新、LayerMask 和协程停止工具方法 | 编译项目；验证迪迦大招等待、战报、激光抖动、命中扣血、墙体阻挡、持续时间和正常结束恢复移动 | 按 `Codex/阶段8-第四批-diff.patch` 回滚 `DiJiaRole.cs`，并恢复本批 Markdown 改动 |
 | 2026-06-17 | 阶段 8 第五批 | `Assets/人物信息/绊线保安/BanXianBaoAnRole.cs`、`Codex/阶段8-第五批-绊线保安结构整理.md`、`Codex/阶段8-第五批-diff.patch`、`Codex/安全重构计划.md`、`Codex/修改记录.md` | 复杂英雄结构整理/常量提取/文档/diff | 否 | 中低 | 整理绊线保安持续射线和折线流程，提取常量、设备收放、命中查找、折线刷新和协程停止工具方法 | 编译项目；验证绊线发射、墙体法线方向、延伸、命中日志、减速音效、折线跟随、10 点伤害和设备收回 | 按 `Codex/阶段8-第五批-diff.patch` 回滚 `BanXianBaoAnRole.cs`，并恢复本批 Markdown 改动 |
+| 2026-06-17 | 阶段 0-8 收口 | `Codex/阶段0到8重构收口总结与上手指南.md`、`Codex/安全重构计划.md`、`Codex/修改记录.md` | 文档/计划收口/上手指南 | 否 | 低 | 正式结束阶段 0 到阶段 8 安全重构计划，并整理后续上手、新增英雄、新增武器、新增机制的入口 | 无需运行游戏；阅读收口指南并按后续专项继续记录 | 删除或恢复本批新增/修改的 Markdown 内容 |
 
 ## 后续记录模板
 
diff --git a/Codex/安全重构计划.md b/Codex/安全重构计划.md
index 3135cb1..ad5773f 100644
--- a/Codex/安全重构计划.md
+++ b/Codex/安全重构计划.md
@@ -4,6 +4,13 @@
 适用项目：DouyinVideo  
 主场景：`Assets/Scenes/VideoScene.unity`
 
+## 当前状态
+
+阶段 0 到阶段 8 已于 2026-06-17 收口，作为一轮完整安全重构计划结束。  
+后续如继续推进，建议按“新增英雄专项 / 新增武器专项 / 娱乐机制专项 / 录制稳定性专项”等新目标重新开计划，而不是默认继续追加阶段编号。
+
+收口总结和上手入口见：`Codex/阶段0到8重构收口总结与上手指南.md`。
+
 ## 目标
 
 在不改变现有自动乱斗玩法的前提下，逐步提升项目的稳定性、可维护性、可复用性和后续扩展能力。
@@ -233,4 +240,4 @@
 - 阶段 8 第三批已完成 `FireWallFireManRole.cs` 的低风险结构整理和配置保护，没有改伤害、时间、射线规则和战报频率。
 - 阶段 8 第四批已完成 `DiJiaRole.cs` 的小步结构整理，提取激光方向、命中判断、线段刷新、LayerMask 和协程停止工具方法，没有改大招时序、数值、射线规则或 `otherBaseRole` 访问节奏。
 - 阶段 8 第五批已完成 `BanXianBaoAnRole.cs` 的小步结构整理，保留 `RaycastAll` 和命中顺序，只提取常量、设备收放、命中查找、折线刷新和协程停止工具方法。
-- 如继续阶段 8，建议只做 `GangsuoRole.cs` 文档级风险复查，暂不直接改钢索核心玩法链路；也可以进入下一阶段做新增英雄/机制前的规范收口。
+- 阶段 8 已随本轮重构计划一起收口；如后续处理 `GangsuoRole.cs`，建议作为新的高风险专项先做文档级风险复查，暂不直接改钢索核心玩法链路。
diff --git b/Codex/阶段0到8重构收口总结与上手指南.md b/Codex/阶段0到8重构收口总结与上手指南.md
new file mode 100644
index 0000000..cb5247f
--- /dev/null
+++ b/Codex/阶段0到8重构收口总结与上手指南.md
@@ -0,0 +1,205 @@
+# 阶段 0 到阶段 8 重构收口总结与上手指南
+
+日期：2026-06-17  
+主场景：`Assets/Scenes/VideoScene.unity`
+
+## 当前结论
+
+阶段 0 到阶段 8 的安全重构计划到此收口。  
+本轮目标不是重写项目，而是在保持自动乱斗玩法不变的前提下，建立文档追踪、降低部分维护成本，并给后续新增英雄、武器、娱乐机制留出更清楚的入口。
+
+本轮没有做的事情同样重要：
+
+- 没有重写 `Battle / Enemy / Tower / UI / Economy` 这类核心系统。
+- 没有改变胜负条件、角色自动弹动规则、武器刷新规则、大招点规则。
+- 没有批量修改 Prefab、Scene 或组件绑定。
+- 没有把所有英雄大招强行改成统一模板。
+- 没有引入 `BaseWeapon`。
+- 没有把 `BaseRole` 拆成多个组件。
+- 没有把 `RaycastAll` 批量替换为 `NonAlloc`。
+
+## 阶段成果总览
+
+| 阶段 | 主要内容 | 结果 |
+|---|---|---|
+| 阶段 0 | 建立文档、修改记录、验证清单、注释规范 | 后续每次改动都有记录入口 |
+| 阶段 1 | 低风险稳定性整理 | 修复少量明确问题，减少部分重复查找和硬编码风险 |
+| 阶段 2 | 基础能力工具化 | 新增目标选择、瞄准、战斗结果等工具，降低复制粘贴 |
+| 阶段 3 | 正常模式保护与娱乐模式预留 | 明确娱乐机制不要污染正常模式，并新增默认关闭的上厕所机制试点脚本 |
+| 阶段 4 | 新增英雄模板与旧英雄小步整理 | 固化英雄脚本结构，整理部分中低风险英雄 |
+| 阶段 5 | 武器系统整理 | 整理普通武器、子弹边界、武器模板，并决定暂不引入 `BaseWeapon` |
+| 阶段 6 | 性能与录制稳定性 | 完成音效对象池试点，审查并暂缓战报池化、大招球池化等高风险项 |
+| 阶段 7 | `BaseRole` 瘦身 | 做注释、分区、私有方法提取和状态图标重复逻辑整理，暂不拆组件 |
+| 阶段 8 | 录制稳定性与复杂英雄结构整理 | 完成 UI 流光对象池，整理火墙火男、迪迦、绊线保安，保留高风险射线语义 |
+
+## 你现在应该先看哪些文档
+
+如果只是想上手当前项目，建议按这个顺序看：
+
+| 顺序 | 文档 | 用途 |
+|---|---|---|
+| 1 | `Codex/当前项目结构.md` | 快速理解项目有哪些主要系统 |
+| 2 | `Codex/安全重构计划.md` | 看本轮重构边界和哪些地方暂缓 |
+| 3 | `Codex/修改记录.md` | 追踪每次具体改了什么 |
+| 4 | `Codex/人工验证清单.md` | 每次你手动跑 Unity 时照着验证 |
+| 5 | `Codex/新增英雄指南.md` | 新增或整理英雄时使用 |
+| 6 | `Codex/新增武器指南.md` | 新增武器时使用 |
+| 7 | `Codex/新增玩法机制指南.md` | 新增娱乐机制或场地机制时使用 |
+
+如果只是正常录制，不打算新增内容：
+
+1. 打开 `Assets/Scenes/VideoScene.unity`。
+2. 点击运行，观察 Console 是否有新增错误。
+3. 按 `Codex/人工验证清单.md` 跑一局完整自动对战。
+4. 重点观察：角色移动、吃大招球、武器刷新、战报、音效、死亡胜利。
+
+## 后续新增英雄怎么做
+
+新增英雄仍然继承 `BaseRole`。不要新建另一套角色父类。
+
+推荐流程：
+
+| 步骤 | 操作 |
+|---|---|
+| 1 | 先读 `Codex/新增英雄指南.md` |
+| 2 | 找一个最像的新英雄参考，不要直接复制高风险复杂英雄 |
+| 3 | 新建英雄脚本，继承 `BaseRole`，只重写 `Big()` |
+| 4 | 按“字段配置 / 运行时状态 / 大招入口 / 大招流程 / 工具方法 / 调试入口”整理结构 |
+| 5 | Inspector 字段加 `[Tooltip("中文说明")]` |
+| 6 | 需要目标时优先用 `otherBaseRole` 或 `VideoGameTargetUtility.GetRandomOther` |
+| 7 | 需要瞄准时优先用 `VideoGameAimUtility` |
+| 8 | 需要伤害、治疗、减速、禁大招、禁武器和播报时优先用 `VideoGameCombatUtility` |
+| 9 | 做 Prefab 和场景绑定时，确认 `RoleName`、`HP`、头像、音效、Collider、Rigidbody2D、Tag |
+| 10 | 如果该英雄要参与实际对战，确认加入 `VideoGameManager.circleRoles` 和对应 `roleColors` |
+| 11 | 如果你的录制流程还使用随机选择展示，确认加入 `RandomChoose.baseRoles` |
+| 12 | 新增后记录到 `Codex/修改记录.md` 并按人工验证清单跑局 |
+
+新增英雄时不要做：
+
+- 不要改 `BaseRole.UseBig()` 的顺序。
+- 不要改 `BaseRole.TakeDamage()` 的扣血、UI、死亡判断顺序。
+- 不要把娱乐机制写进每个英雄。
+- 不要为了复用把多个大招硬合并成一个巨大的父类。
+- 不要一次新增多个英雄又同时改核心系统。
+
+## 后续新增武器怎么做
+
+普通枪械优先走模板，不急着做父类。
+
+推荐流程：
+
+| 步骤 | 操作 |
+|---|---|
+| 1 | 先读 `Codex/新增武器指南.md` |
+| 2 | 普通枪械复制 `Codex/模板-普通枪械武器.cs.txt` 作为起点 |
+| 3 | 特殊武器先填写 `Codex/模板-特殊武器设计卡片.md` |
+| 4 | 拾取逻辑保持检查 `Role` 和 `canGetWeapon` |
+| 5 | 挂载、瞄准、翻转优先用 `VideoGameWeaponUtility` 和 `VideoGameAimUtility` |
+| 6 | 子弹优先用 `VideoGameBulletBase.Init` 和回调 |
+| 7 | 命中伤害和播报优先用 `VideoGameCombatUtility` |
+| 8 | 武器结束后恢复状态、隐藏对象，并按原流程刷新下一件武器 |
+| 9 | 如果要进入随机刷新池，确认加入 `VideoGameManager.Weapons` |
+| 10 | 新增后记录到 `Codex/修改记录.md` 并验证拾取、发射、命中、回收、刷新 |
+
+新增武器时不要做：
+
+- 不要现在引入 `BaseWeapon`。
+- 不要改 `VideoGameManager.SpawnWeapons()` 的刷新节奏。
+- 不要让命中墙体和命中角色的回收边界混在一起。
+- 不要把英雄大招生成物直接塞进普通武器刷新列表。
+
+## 后续新增玩法机制怎么做
+
+玩法机制分两类：
+
+| 类型 | 说明 |
+|---|---|
+| 正常机制 | 你希望以后默认出现在正常录制里的规则 |
+| 娱乐机制 | 上厕所、随机事件、场地机关这类观众爱看的附加规则 |
+
+推荐流程：
+
+| 步骤 | 操作 |
+|---|---|
+| 1 | 先在 `Codex/` 写机制设计说明 |
+| 2 | 明确它是正常机制还是娱乐机制 |
+| 3 | 明确默认是否启用 |
+| 4 | 明确影响哪些对象：所有英雄、部分英雄、场地、UI 或武器 |
+| 5 | 优先做独立 Manager / 状态组件 / 区域触发器 |
+| 6 | 不把机制状态直接写进 `BaseRole` |
+| 7 | 扣血调用 `BaseRole.TakeDamage` 或 `VideoGameCombatUtility` |
+| 8 | 停速、减速调用 `BaseRole.SetSpeed` 或工具方法 |
+| 9 | 播报走 `Broadcast` 或 `VideoGameCombatUtility` |
+| 10 | Inspector 只暴露关键开关和少量参数 |
+| 11 | 默认关闭的机制，未挂入场景或未启用时必须完全不影响正常模式 |
+| 12 | 新增后记录到 `Codex/修改记录.md` 并验证完整对局 |
+
+当前已有参考：
+
+- `Assets/Scripts/Entertainment/RoleToiletState.cs`
+- `Assets/Scripts/Entertainment/ToiletArea.cs`
+- `Assets/Scripts/Entertainment/ToiletMechanismManager.cs`
+- `Codex/阶段3-第四批-上厕所机制最小试点.md`
+
+这些脚本是默认关闭的试点，不代表已经正式接入正常玩法。
+
+## 当前可以放心复用的工具
+
+| 工具 | 用途 |
+|---|---|
+| `VideoGameTargetUtility` | 随机目标选择 |
+| `VideoGameAimUtility` | 朝向、方向、翻转 |
+| `VideoGameCombatUtility` | 伤害、播报、减速、禁用状态 |
+| `VideoGameWeaponUtility` | 普通武器挂载、瞄准、隐藏 |
+| `VideoGameBulletBase` | 通用子弹命中和回收边界 |
+
+这些工具只做基础能力，不替你决定玩法规则。  
+新英雄、新武器、新机制的特色仍然写在自己的脚本里。
+
+## 当前高风险区域
+
+后续除非有明确目标和单独验证计划，否则不要直接改：
+
+| 区域 | 原因 |
+|---|---|
+| `BaseRole.FixedUpdate()` | 影响所有角色自动弹动 |
+| `BaseRole.TakeDamage()` | 影响扣血、UI、死亡和胜负判断 |
+| `BaseRole.UseBig()` / `AddBig()` | 影响所有大招触发顺序 |
+| `VideoGameManager.SpawnBigBall()` | `bigBallPrefab` 直接参与大招点触发链路 |
+| `VideoGameManager.SpawnWeapons()` | 影响武器刷新节奏 |
+| `Broadcast` 对象池化 | 会改变战报历史保留规则 |
+| `RaycastAll -> RaycastNonAlloc` | 可能改变命中顺序和目标选择 |
+| `GangsuoRole.cs` | 直接移动目标、禁用碰撞体、改大招状态，风险高 |
+| 大批量 Prefab / Scene 修改 | 难以从代码 diff 看出完整影响 |
+
+## 每次修改后的最低记录要求
+
+不管是代码、文档、Prefab 还是机制接入，都建议至少记录：
+
+| 项目 | 要写清楚 |
+|---|---|
+| 修改文件 | 具体路径 |
+| 是否改变玩法 | 是 / 否 |
+| 风险等级 | 低 / 中 / 高 |
+| 修改原因 | 为什么要改 |
+| 验证点 | 你要在 Unity 里看什么 |
+| 回滚建议 | 出问题先退哪个文件 |
+
+记录位置：
+
+- `Codex/修改记录.md`
+
+## 当前计划结束状态
+
+阶段 0 到阶段 8 已作为一轮完整安全重构计划结束。
+
+之后如果继续推进，建议不要默认叫“阶段 9”。  
+更推荐按目标重新开计划，例如：
+
+- 新增英雄专项。
+- 新增武器专项。
+- 娱乐机制专项。
+- 录制稳定性专项。
+- 高风险核心系统专项。
+
+这样比继续无限追加阶段更容易追踪，也更适合你后续频繁新增内容。
